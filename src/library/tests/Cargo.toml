# General info
[package]
name = "libwhitebeam-tests"
version = "0.2.2"
authors = ["WhiteBeam Security, Inc."]
edition = "2018"

# Build targets
[[bin]]
name = "libwhitebeam-tests"
path = "main.rs"

# Cross-platform dependencies
[dependencies]
libc = { version = "^0.2.90" }
